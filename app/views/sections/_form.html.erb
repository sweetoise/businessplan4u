<%= simple_form_for((@section), html: { class: "form-horizontal"}) do |f| %>

<% if @section.errors.any? %>
<div class="box-body">
	<div class="callout callout-warning">
		<h4><i class="icon fa fa-warning"></i> Error Alert!</h4>
	</div>
</div><!-- /.box-body -->
<% end %>

<% if @section.errors.any?  %>
<ul>
	<% @section.errors.full_messages.each do |msg| %>
	<li>
		<%= msg %>
	</li>
	<% end %>
</ul>
<% end %>

<div class="modal-body">
	<div class="box-body">
		<!-- Horizontal Form -->
		<!-- form start -->
		<div class="box-body">
			<div class="form-group">
				<label for="inputEmail3" class="col-sm-4 control-label">Business Plan</label>
				<div class="col-sm-7">
					<%= f.collection_select(:sample_business_plan_id,  @sample_business_plans,  :id, :title, {:prompt   => "Select Business Plan", :selected => params[:sample_business_plan_id]}, {:id => 'sample_business_plans_select', :class => 'form-control'}) %>
				</div>
			</div>
			<div class="form-group">
				<label for="inputEmail3" class="col-sm-4 control-label">Section Name</label>
				<div class="col-sm-7">
					<%= f.input :name, :label => false, :placeholder => "Enter Name ", input_html: { class: 'form-control form-control-input', id: 'inputEmail3' } %>
				</div>
			</div>
		</div>
	</div><!-- /.box-body -->
</div>
<div class="modal-footer" style="padding-right: 13%; padding-left: 13%;">
	<div class="form-actions">
		<%= link_to 'Back', sections_path, class: 'btn btn-default pull-left' %>
	</div>
	<div class="form-actions">
		<%= f.button :submit, class: 'btn btn-primary' %>
	</div>
</div>
</div><!-- /.modal-content -->
<% end %>
